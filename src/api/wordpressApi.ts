/**
 * Backward-compatible barrel.
 * All API functions and types have been split into domain modules.
 * New code should import from '@/api' or from the specific module (e.g. '@/api/content').
 */
export * from '.';
